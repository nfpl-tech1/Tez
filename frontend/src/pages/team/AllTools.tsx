import { TeamLayout } from '@/components/layout/TeamLayout';
import { AllToolsList } from '@/components/shared';

export default function AllTools() {
    return <AllToolsList Layout={TeamLayout} />;
}
