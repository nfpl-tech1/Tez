import { AdminLayout } from '@/components/layout/AdminLayout';
import { AllToolsList } from '@/components/shared';

export default function AdminAllTools() {
    return <AllToolsList Layout={AdminLayout} />;
}
